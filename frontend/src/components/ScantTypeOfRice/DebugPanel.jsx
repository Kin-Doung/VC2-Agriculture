import { Card, CardHeader, CardTitle, CardContent } from "../ui/Card";

export default function DebugPanel({ selectedImage, result, error, scanHistory }) {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Debug Panel</CardTitle>
      </CardHeader>

    </Card>
  );
}